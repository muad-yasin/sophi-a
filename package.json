{
  "name": "sophi-a",
  "private": true,
  "version": "0.1.0",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "package:orchestrator": "esbuild src/orchestrator/index.js --bundle --platform=node --format=esm --outfile=dist-orchestrator/index.js --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" && cp src/orchestrator/seats.json dist-orchestrator/seats.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "diff": "^9.0.0",
    "ws": "^8.21.3",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.28.2",
    "typescript": "~6.0.3",
    "vite": "^8.0.16"
  }
}
